import React from "react";
interface TableProps {
    tabelData?: string;
    tableHeaderColumn?: string;
}
export default function Table({ tabelData, tableHeaderColumn }: TableProps): React.JSX.Element;
export {};
